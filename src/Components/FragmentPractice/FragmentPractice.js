import React from 'react';

const FragmentPractice = () => {
    return (
        <React.Fragment>
            <h2>hello this is from react fragment </h2>   
        </React.Fragment>
    );
};

export default FragmentPractice;